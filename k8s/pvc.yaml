apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-local-pvc
  namespace: claude-local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard   # KIND uses 'standard' by default
