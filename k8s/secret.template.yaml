# ⚠️  THIS IS A TEMPLATE ONLY — NEVER COMMIT REAL VALUES TO GIT
# ⚠️  Use scripts/create-secret.sh to create this secret on your cluster instead
#
# This file is intentionally left with placeholder values.
# Your real API key is created directly on the cluster via kubectl.

apiVersion: v1
kind: Secret
metadata:
  name: claude-local-secrets
  namespace: claude-local
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME_DO_NOT_COMMIT"
